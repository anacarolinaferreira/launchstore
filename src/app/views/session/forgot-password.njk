{% extends "layout.njk"%}

{%import "parts/cards.njk" as card%}

{% block content %}

  {%call card.render('Esqueceu a senha?', 'password container')%}
    <form action="/users/forgot-password" method="POST">

      <div class="fields">
        <div>Email</div>
        <div class="input">
          <input 
            type="email" 
            name="email" 
            placeholder="Digite o email cadastrado" 
            value="{{user.email}}" 
            onblur="Validate.apply(this, 'isEmail')" {# o blur é usado quando o usuário sai do campo. ex: clica em outro input ou na tela #} required="required">
        </div>
      </div>
    </div>

    <button class="button" type="submit">Recuperar senha</button>
  </form>

{%endcall%}
{%endblock content %}